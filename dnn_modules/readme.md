# python package for DNN training tools

- keras callback for monitor history using matplotlib
- plot confusion matrix
- get best model(.hdf5) and model name in the saved models by ModelCheckpoint callback